#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [[ -x "${REPO_ROOT}/venv/bin/python" ]]; then
  exec "${REPO_ROOT}/venv/bin/python" -m pip "$@"
fi

exec python3 -m pip "$@"

