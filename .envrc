# Auto-activate the repo virtualenv when you enter this directory (requires direnv).
#
# One-time setup (macOS/Linux):
#   1) Install direnv: https://direnv.net
#   2) Enable it for zsh:
#        echo 'eval "$(direnv hook zsh)"' >> ~/.zshrc
#        echo 'eval "$(direnv hook zsh)"' >> ~/.zprofile
#   3) Allow this repo:
#        direnv allow
#
# Notes:
# - This affects interactive shells that have direnv enabled.
# - For non-interactive shells (some CLI agents), prefer `bash scripts/py ...`
#   or `./venv/bin/python ...` explicitly.

if [ -f "venv/bin/activate" ]; then
  # Avoid re-activating if we’re already using this repo venv.
  if [ "${VIRTUAL_ENV-}" != "${PWD}/venv" ]; then
    . "venv/bin/activate"
  fi
fi

